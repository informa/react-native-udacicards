import * as React from "react";
import { Text, View } from "react-native";

const QuizScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 40 }}>Quiz Screen!</Text>
    </View>
  );
};

export default QuizScreen;
